<UserControl x:Class="Reflector.ReflectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reflector"
             xmlns:conv="clr-namespace:SolutionExtensions"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignData /_DesignData/ReflectorRoot.xaml}"    
             >
    <Grid>
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="boolToVisi"/>
            <conv:EmptyConverter x:Key="notEmptyToVisi" Inner="{StaticResource boolToVisi}" Negate="True"/>
            <conv:EmptyConverter x:Key="emptyToVisi" Inner="{StaticResource boolToVisi}"/>
            <conv:EmptyConverter x:Key="notEmpty" Negate="True"/>
            <PathGeometry x:Key="TreeArrow" Figures="M0,0 L0,6 L6,0 z"/>
            <SolidColorBrush x:Key="TreeViewItem.TreeArrow.Static.Stroke" Color="#FF818181"/>
            <SolidColorBrush x:Key="TreeViewItem.TreeArrow.Static.Fill" Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="TreeViewItem.TreeArrow.MouseOver.Stroke" Color="#FF27C7F7"/>
            <SolidColorBrush x:Key="TreeViewItem.TreeArrow.MouseOver.Fill" Color="#FFCCEEFB"/>
            <SolidColorBrush x:Key="TreeViewItem.TreeArrow.Static.Checked.Stroke" Color="#FF262626"/>
            <SolidColorBrush x:Key="TreeViewItem.TreeArrow.Static.Checked.Fill" Color="#FF595959"/>
            <SolidColorBrush x:Key="TreeViewItem.TreeArrow.MouseOver.Checked.Stroke" Color="#FF1CC4F7"/>
            <SolidColorBrush x:Key="TreeViewItem.TreeArrow.MouseOver.Checked.Fill" Color="#FF82DFFB"/>
            <Style x:Key="expanderStyle" TargetType="{x:Type ToggleButton}">
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="Width" Value="16"/>
                <Setter Property="Height" Value="16"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border Background="Transparent" Height="16" Padding="5,5,5,5" Width="16">
                                <Path x:Name="ExpandPath" 
                                      Data="{StaticResource TreeArrow}" 
                                      Fill="{StaticResource TreeViewItem.TreeArrow.Static.Fill}" 
                                      Stroke="{StaticResource TreeViewItem.TreeArrow.Static.Stroke}">
                                    <Path.RenderTransform>
                                        <RotateTransform Angle="135" CenterY="3" CenterX="3"/>
                                    </Path.RenderTransform>
                                </Path>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="RenderTransform" TargetName="ExpandPath">
                                        <Setter.Value>
                                            <RotateTransform Angle="180" CenterY="3" CenterX="3"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Fill" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.Static.Checked.Fill}"/>
                                    <Setter Property="Stroke" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.Static.Checked.Stroke}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Stroke" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.MouseOver.Stroke}"/>
                                    <Setter Property="Fill" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.MouseOver.Fill}"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                        <Condition Property="IsChecked" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Stroke" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.MouseOver.Checked.Stroke}"/>
                                    <Setter Property="Fill" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.MouseOver.Checked.Fill}"/>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="tvItemContainer" TargetType="TreeViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TreeViewItem">
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <ToggleButton x:Name="Expander" ClickMode="Press" 
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource Mode=TemplatedParent}}" 
                                              Style="{StaticResource expanderStyle}"
                                              />
                                <ContentPresenter x:Name="PART_Header" Grid.Column="1"
                                                  ContentSource="Header" 
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                <ItemsPresenter x:Name="ItemsHost" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsExpanded" Value="false">
                                    <Setter Property="Visibility" TargetName="ItemsHost" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="HasItems" Value="false">
                                    <Setter Property="Visibility" TargetName="Expander" Value="Hidden"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="true">
                                    <!--<Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>-->
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsSelected" Value="true"/>
                                        <Condition Property="IsSelectionActive" Value="false"/>
                                    </MultiTrigger.Conditions>
                                    <!--<Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}"/>-->
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}}"/>
                                </MultiTrigger>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <ControlTemplate TargetType="ContentControl" x:Key="tvItemTemplate">
                <Border BorderBrush="{TemplateBinding Background}" 
                        HorizontalAlignment="Stretch"
                        Background="{TemplateBinding Background}" 
                        BorderThickness="1" CornerRadius="4" Padding="10,5">
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colButtons"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter/>
                        <StackPanel Orientation="Horizontal" Grid.Column="1"
                                    Margin="10,0,0,0">
                            <StackPanel.Resources>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Width" Value="30"/>
                                    <Setter Property="FontFamily" Value="Courier New"/>
                                    <Setter Property="Margin" Value="0,0,5,0"/>
                                </Style>
                            </StackPanel.Resources>
                            <Button Content=".p" Visibility="{Binding CanExpandProperties, Converter={StaticResource boolToVisi}}" Click="ExpandProperties_Click"/>
                            <Button Content="{}{i}" Visibility="{Binding CanExpandInterfaces, Converter={StaticResource boolToVisi}}" Click="ExpandInterfaces_Click"/>
                            <Button Content="m()" Visibility="{Binding CanExpandMethods, Converter={StaticResource boolToVisi}}" Click="ExpandMethods_Click"/>
                            <Button Content="[e]" Visibility="{Binding CanExpandEnumerable, Converter={StaticResource boolToVisi}}" Click="ExpandEnumerable_Click"/>
                            <Button Content="c#" Click="Generate_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </ControlTemplate>
            <Style TargetType="ContentControl" x:Key="tvItem">
                <Setter Property="Template" Value="{StaticResource tvItemTemplate}"/>
            </Style>
            <Style x:Key="ico" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Courier New"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style x:Key="code" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Courier New"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <HierarchicalDataTemplate 
                DataType="{x:Type local:ReflectorRoot}"
                ItemsSource="{Binding Path=Children}">
                <ContentControl Style="{StaticResource tvItem}" Background="#80ff80">
                    <TextBlock Grid.Column="1" Text="{Binding RootType}"/>
                </ContentControl>
            </HierarchicalDataTemplate>
            <HierarchicalDataTemplate 
                DataType="{x:Type local:ReflectorPropertyValue}"
                ItemsSource="{Binding Path=Children}">
                <ContentControl Style="{StaticResource tvItem}" Background="#80ff80">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colIco"/>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colName"/>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="(p)" Style="{StaticResource ico}"/>
                        <TextBlock Text="{Binding PropertyName}" FontWeight="Bold" Grid.Column="1"/>
                        <StackPanel Orientation="Horizontal" Grid.Column="2">
                            <TextBlock Visibility="{Binding ValueSimpleText,Converter={StaticResource notEmptyToVisi}}" Margin="0,0,5,0">
                                =<Run Text="{Binding ValueSimpleText}"/>
                            </TextBlock>
                            <TextBlock Visibility="{Binding IsNull,Converter={StaticResource boolToVisi}}" Margin="0,0,5,0">
                                =<Run Text="(null)"/>
                            </TextBlock>
                            <TextBlock Text="{Binding PropertyTypeName}" Style="{StaticResource code}" FontSize="10"/>
                        </StackPanel>
                    </Grid>
                </ContentControl>
            </HierarchicalDataTemplate>
            <HierarchicalDataTemplate 
                DataType="{x:Type local:ReflectorMethod}"
                ItemsSource="{Binding Path=Children}">
                <ContentControl Style="{StaticResource tvItem}" Background="#8080ff">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colIco"/>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colName"/>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="(m)" Style="{StaticResource ico}"/>
                        <TextBlock Text="{Binding MethodName}" Grid.Column="1" FontWeight="Bold" />
                        <TextBlock Text="{Binding Signature}" Grid.Column="2" Style="{StaticResource code}"/>
                    </Grid>
                </ContentControl>
            </HierarchicalDataTemplate>
            <HierarchicalDataTemplate 
                DataType="{x:Type local:ReflectorEnumItem}"
                ItemsSource="{Binding Path=Children}">
                <ContentControl Style="{StaticResource tvItem}" Background="#40ff40">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colIco"/>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colName"/>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Index, StringFormat={}[{0}]}" Style="{StaticResource ico}"/>
                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                            <TextBlock Text="{Binding ValueSimpleText}"/>
                            <TextBlock Text="{Binding ValueTypeName}" FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>
                </ContentControl>
            </HierarchicalDataTemplate>
            <HierarchicalDataTemplate 
                DataType="{x:Type local:ReflectorInterface}" 
                ItemsSource="{Binding Path=Children}">
                <ContentControl Style="{StaticResource tvItem}" Background="#FFFF00">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colIco"/>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="colName"/>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{}{intf}" Style="{StaticResource ico}"/>
                        <TextBlock Text="{Binding ValueTypeName}" Grid.Column="2" FontWeight="Bold" Style="{StaticResource code}"/>
                    </Grid>
                </ContentControl>
            </HierarchicalDataTemplate>
        </Grid.Resources>
        <TreeView x:Name="treeView" 
                  ItemsSource="{Binding Children}" Grid.IsSharedSizeScope="True"
                  ItemContainerStyle="{StaticResource tvItemContainer}">
        </TreeView>
    </Grid>
</UserControl>
