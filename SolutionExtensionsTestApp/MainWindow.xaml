<Window x:Class="SolutionExtensionsTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SolutionExtensionsTestApp"
        xmlns:themes="clr-namespace:SolutionExtensions.UI.Themes;assembly=SolutionExtensions.UI"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="grid">
        <Grid.LayoutTransform>
            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
        </Grid.LayoutTransform>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="99" Margin="5,3">
            <Button Click="Zoom_Click">Zoom</Button>
            <Button Click="DumpTheme_Click">Dump theme</Button>
            <Button Background="{StaticResource {x:Static themes:ThemeKeys.TreeViewItem_Stroke}}">Color</Button>
            <Button>
                <Rectangle Width="50" Height="20" 
                           Fill="{StaticResource {x:Static themes:ThemeKeys.TreeViewItem_TreeArrow_Static_Stroke}}">
                    
                </Rectangle>
            </Button>
        </StackPanel>
        <local:ExtListToolWindow Grid.Row="1" Grid.Column="0"/>
        <GridSplitter Grid.Column="1" Grid.RowSpan="2" 
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"/>
        <local:ReflToolWindow Grid.Row="1" Grid.Column="2"/>
    </Grid>
</Window>
