<Window x:Class="SolutionExtensions._DesignData.ColorWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SolutionExtensions._DesignData"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignData ColorWindowVM.xaml}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type local:ColorModel}">
                <Border Width="20" Height="20" Margin="3,3" Background="{Binding Brush}">
                    <Border.ToolTip>
                        <TextBlock>
                            <Run Text="{Binding Name,Mode=OneWay}"/>
                            <LineBreak/>
                            <Run Text="{Binding DeclaringTypeName,Mode=OneWay}"/>
                            <LineBreak/>
                            <Run Text="{Binding CategoryName,Mode=OneWay}"/>
                            <LineBreak/>
                            <Run Text="{Binding Category,Mode=OneWay}"/>
                        </TextBlock>
                    </Border.ToolTip>
                </Border>
            </DataTemplate>

        </Grid.Resources>
        <TabControl>
            <TabItem>
                <TabItem.Header>
                    <TextBlock>
                        <Bold>VSColors</Bold>
                        <LineBreak/>
                        <Run FontSize="9">Microsoft.VisualStudio.Shell</Run>
                    </TextBlock>
                </TabItem.Header>
                <ItemsControl ItemsSource="{Binding Colors}" ScrollViewer.VerticalScrollBarVisibility="Visible">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <TextBlock>
                        <Bold>PlatformUI classes</Bold>
                        <LineBreak/>
                        <Run FontSize="9">Microsoft.VisualStudio.PlatformUI</Run>
                    </TextBlock>
                </TabItem.Header>
                <ItemsControl ItemsSource="{Binding UIColors}" ScrollViewer.VerticalScrollBarVisibility="Visible">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
